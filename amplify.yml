version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        build:
          commands:
            - echo "Static site - no build step needed"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
      cache:
        paths: []
